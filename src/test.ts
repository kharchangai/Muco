import { getMemoryForAgent  } from "./services/ai/tools/memory/short-memory/memoryGate";

export async function runTest() {
  try {
    //const memoryResult = await getMemoryForAgent("یادت میاد درباره wow چی گفتم؟");

    //console.log(memoryResult)

  } catch (error) {
    console.error("Test failed:", error);
  }
}